
export interface IMetadata {
  artist: string;
  title: string;
};

export interface ISponsor {
  name: string;
  phone?: string;
  whatsapp?: string;
  url?: string;
  description?: string;
  logo?: string;
};